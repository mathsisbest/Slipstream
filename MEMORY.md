<!--
This is the memory index: one line per fact, loaded at the start of a session so the agent
remembers what it learned. Keep each line short — a title, a link, and a hook. The full fact
lives in its own file under memory/ (see memory/TEMPLATE.md). Delete facts that turn out wrong.

Add a line per fact as you create the file under memory/. Each line looks like:

- [Project overview](memory/project-overview.md) — what this repo is, the stack, the one non-obvious setup step
- [Feedback: PR size](memory/feedback-pr-size.md) — keep PRs to one concern; reviewer rejects bundled changes
-->

