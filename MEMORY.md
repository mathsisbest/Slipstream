<!--
This is the memory index: one line per fact, loaded at the start of a session so the agent
remembers what it learned. Keep each line short — a title, a link, and a hook. The full fact
lives in its own file under memory/ (see memory/TEMPLATE.md). Delete facts that turn out wrong.

Replace the examples below with your project's real facts.
-->

- [Project overview](memory/example-project.md) — what this repo is, the stack, the one non-obvious setup step
- [Feedback: PR size](memory/example-feedback.md) — keep PRs to one concern; reviewer rejects bundled changes
